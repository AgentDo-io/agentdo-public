# Changelog

All notable changes to the AgentDo **MCP endpoint and REST API** are documented here.

Internal changes (dashboard, landing copy, server infrastructure) are intentionally omitted — they are not user-visible to AI agent integrators. Only changes to the public API surface (tools, schemas, auth, rate limits, endpoints, breaking behaviour) appear here.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]
